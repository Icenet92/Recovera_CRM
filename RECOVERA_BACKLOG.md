# RECOVERA — Outstanding Fixes Backlog

## How to use this file

Work through the items below **in order, one at a time.** Do not start the next item until the previous one is actually verified working (not just implemented) — check it off with a one-line note on how it was verified. If an item is blocked or partially done, say so explicitly next to it rather than marking it done.

When a new issue is found later, it gets added to the bottom of this list — this file is the single source of truth for "what's still open," not a chat message.

---

- [x] **1. Confirm GitHub push status.** DONE — renamed local branch `master` → `main`; `git remote add origin https://github.com/Icenet92/Recovera_CRM.git`; `GIT_TERMINAL_PROMPT=0 git push -u origin main --force` → `branch 'main' set up to track 'origin/main'` and `+ 5ee890e...2bbbc0d main -> main (forced update)` (replaced GitHub's throwaway `5ee890e` initial commit with the real project history ending at `2bbbc0d`), EXITCODE=0. Authenticated automatically via the cached Git Credential Manager entry for `github.com` (no 401). Verified by push output + `git remote -v` showing `origin` and `git branch` showing `main` tracking `origin/main`.

- [x] **2. Add a "Batches" list screen** (sidebar route) showing all Recovery Assignments — filterable by status (All/Active/Completed/Cancelled), each row clicking through to the existing batch detail screen. VERIFIED: `BatchesScreen` (`lib/screens/recovery/batches_screen.dart`, already scaffolded) is now backed by `RecoveryAssignmentRepository.getAll({bool activeOnly})` + `RecoveryProvider.loadAssignments()` (which calls `getAll(activeOnly: false)` so Completed/Cancelled rows are loadable and filterable client-side). Sidebar `item('Batches', Icons.inventory_2_rounded, requiredPerm: 'assignment.view')` + AppShell `'Batches'` branch wire `onBatchSelected` → `_selectedAssignmentId` → the existing `RecoveryAssignmentDetailScreen` (no new detail screen). `dart analyze lib test` → 9 pre-existing issues, 0 new. `flutter test --timeout 180s` → `00:19 +9: All tests passed!`. Added 2 `testWidgets` to `test/debtors_ui_test.dart`: (A) renders empty state + "Batches" title + 4 status chips (All/Active/Completed/Cancelled) + "New Batch" FilledButton; (B) creates a batch via `recProv.createRecoveryAssignment` with the seeded officer `sam.mugisha` + case `CASE-2026-000001`, asserts the batch's short id appears in the list, then row-taps it and asserts `RecoveryAssignmentDetailScreen` opens (BatchesScreen unmounted). Note: the lookup queries + create + `loadAssignments` in test (B) are wrapped in `tester.runAsync(...)` — a plain `await` under the widget-test's FakeAsync zone hangs on sqflite ffi completions (10-min timeout) — per the file's own header comment. Commit `b8e2a4f` on `main`.

- [x] **3. Case Reassign dialog + Add Supporting Officer dialog** currently show raw "Employee ID" text fields with placeholder "Enter employee ID" — no names, no dropdown. Replace both with a searchable dropdown showing "Full Name — Job Title," backed by the ID underneath, sourced the same way as the batch form's officer picker (which already works correctly — use that as the reference implementation, don't rebuild from scratch). VERIFIED: extracted a single shared `lib/widgets/officer_picker.dart` (`OfficerPicker`, a searchable `TextField`+filtered-`ListView` wrapped in a `FormField` so it participates in `Form.validate()`; shows "Full Name — Job Title" with the user id as the value, falls back to username / "No title" when empty); reused at all 4 call sites — `recovery_assignment_form.dart` officer selector, `reassign_case_dialog.dart` NEW OWNER + SUPERVISOR, and `case_detail_screen.dart` `_addSupportingEmployee` (now wired: the Add button passes the selected id to the existing `prov.addSupportingEmployee(caseId, id)`). No new provider/repo method was needed: the sanity check confirmed `listOfficers()`/`officers`/`addSupportingEmployee()`/`reassignCase(…, supervisor)` already exist and were the reason the analyzer stayed clean. Fixed the one real gap: `RecoveryAssignmentRepository.listRecoveryOfficers()` now also selects `e.job_title AS job_title` (the `employees` table column — note `employees.position` is the `contacts` table, not `employees`; the original `e.position` attempt threw "no such column" and broke `BatchesScreen`). `dart analyze lib test` → 9 pre-existing issues, 0 new. `flutter test --timeout 180s` → `00:20 +9: All tests passed!` (the existing `RecoveryAssignmentForm: empty submission shows validation errors` test renders the new OfficerPicker and asserts 'Please select an officer' — green, so the validator+errorText path is exercised).

- [x] **4. Case Detail screen: "Debtor: Loading..." never resolves.** It was stuck permanently. VERIFIED: root cause was `RecoveryProvider.loadCaseDetails` (recovery_provider.dart) fetched the case + assignments + status-history + supporting employees but never the case's own debtor, so `CaseDetailScreen`'s `recProv.debtors.where(id == cModel.debtorId).firstOrNull` always failed when landing on Case Detail from a client's Cases tab (no prior `loadDebtors`). Fix: `loadCaseDetails` now best-effort fetches the debtor via `_debtorRepo.getById(debtorId)` and appends it to `_debtors` (guarded by an already-present check; the fetch is in try/catch so a lookup failure degrades to the placeholder instead of masking the case fetch). Added regression test `Case Detail debtor resolves via loadCaseDetails` (plain `test()`, not widget) in test/recovery_flow_test.dart. `dart analyze lib test` → 9 pre-existing, 0 new. `flutter test --timeout 180s` → `00:19 +10: All tests passed!` (t_item4.log). Commit f49a1b9 → pushed.

- [x] **5. Batch form case picker scaling.** Currently shows every case in the company as one flat list (case number + title only). Add a Client filter/selector above the list to narrow it down, and show each case's debtor name on its row. Cases from more than one client must remain selectable together in one batch if the manager wants that — this is a filter for convenience, not a restriction to one client per batch. VERIFIED: `recovery_assignment_form.dart` now loads `CrmProvider.organizations` + `RecoveryProvider.debtors` in `autoLoad`, renders a "Client" `DropdownButtonFormField` (All Clients + per-org, `initialValue` to dodge the deprecated `value`) that filters the visible list into `filteredCases`, but the `_caseIds` selection is independent of the filter so multi-client pooling still works. Each `CheckboxListTile` now shows `caseNumber` as the title and a two-line subtitle: debtor name (looked up from `c.debtorId` via `_debtorName`) over the case title. `dart analyze lib test` → 9 pre-existing, 0 new. `flutter test --timeout 180s` → `00:20 +10: All tests passed!` (the form's empty-submission test still green with the new dropdown + rows).

- [x] **6. Batch form Target Amount clarity + verify override works.** VERIFIED: added `helperText: 'Auto-suggested from total outstanding — adjust as needed.'` to the Target `TextFormField` (with key `target_amount`). Override-persistence fixed: `recovery_assignment_form.dart`'s `_targetCtrl` listener now sets `_targetDirty` only on genuine user edits — programmatic writes in `_recomputeTarget` (`_targetCtrl.text = total.toStringAsFixed(0)`) are wrapped in a `_suppressDirty` guard so they don't flip `_targetDirty`. That (a) keeps auto-summing LIVE across multiple case toggles (the latent bug: the prior write flipped `_targetDirty`, then `if (_targetDirty) return` froze the total after the first toggle — so the field ignored further toggling, not a silent revert on save) and (b) lets a manual edit persist untouched when cases are toggled afterward. `_save` already persisted `double.parse(_targetCtrl.text)` (unchanged). Added regression test `RecoveryAssignmentForm: target auto-sums across toggles and manual override persists` in test/debtors_ui_test.dart: selects 2 open cases (outstanding 5000000 + 250000) → asserts target sums to `5250000` (proves auto-sum stays live across toggles = the `_suppressDirty` fix), enters `99999`, toggles a case off → asserts target stays `99999` (manual override persists). `dart analyze lib test` → 9 pre-existing, 0 new. `flutter test --timeout 180s` → `00:21 +11: All tests passed!` (t_item6.log).
